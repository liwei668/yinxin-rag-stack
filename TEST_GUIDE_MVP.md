# 银信RAG堆栈 - 第一阶段MVP功能测试指南

## 测试环境
- 访问地址: http://localhost:3000 (或 http://localhost:3001)
- 管理员账号: liwei
- 管理员密码: admin123

## 测试步骤

### 1. 登录管理后台
1. 打开浏览器访问 http://localhost:3000/admin
2. 使用管理员账号登录
3. 验证成功进入管理后台

### 2. 测试客户档案管理
**路径**: 左侧菜单 → "客户"

**测试项目**:
- ✅ 查看客户列表（应为空或显示已有客户）
- ✅ 新增客户
  - 客户ID: `test-001`
  - 公司名称: `测试公司`
  - 联系人: `张三`
  - 邮箱: `test@example.com`
  - 行业: `科技`
- ✅ 编辑客户信息
- ✅ 编辑客户详细档案（TXT文件）
  - 点击"编辑档案"按钮
  - 在编辑器中添加内容：
    ```
    [客户画像]
    公司名称：测试公司
    行业：科技
    关注：AI应用
    
    [对话记录]
    客户咨询了AI功能
    
    [待办事项]
    1. 提供演示
    2. 跟进报价
    ```
  - 保存档案
- ✅ 验证档案加载（重新打开档案编辑器，检查内容）
- ✅ 删除测试客户

**预期结果**:
- 所有CRUD操作成功
- TXT档案编辑和保存正常
- 搜索功能可用

### 3. 测试模型路由管理
**路径**: 左侧菜单 → "模型"

**测试项目**:
- ✅ 查看现有路由规则
  - 应该看到4条预设规则：
    1. 默认-快速模式 (default)
    2. 财务分析规则 (keyword)
    3. 文档分析规则 (fileType)
    4. 复杂推理规则 (intent)
- ✅ 测试路由匹配
  - 输入关键词"财务报表"
  - 验证系统选择了 deepseek-v4-pro
- ✅ 创建新规则
  - 名称: `测试关键词规则`
  - 类型: `keyword`
  - 关键词: `测试,示例`
  - 目标模型: `deepseek-v4-flash`
  - 优先级: `50`
  - 激活状态: `是`
- ✅ 编辑规则
- ✅ 删除测试规则

**预期结果**:
- 规则列表显示正确
- 路由匹配准确
- 规则管理功能完整

### 4. 测试技能提词器组合
**注意**: 技能提词器目前集成在模型管理或内容管理模块中

**测试项目**:
- ✅ 查看技能组合列表
  - 销售咨询组合
  - 技术支持组合
  - 财税法分析组合
- ✅ 测试组合逻辑
  - 选择"销售咨询组合"
  - 验证组合后的提词器内容

**预期结果**:
- 组合列表正确显示
- 技能文件可以正确加载
- 组合内容拼接正常

### 5. 测试错误处理和降级
**测试场景**:
- ✅ 无匹配规则时
  - 输入不包含任何关键词的文本
  - 验证系统使用默认模型（deepseek-v4-flash）
- ✅ 路由规则异常
  - 验证错误日志记录
  - 验证系统不会崩溃
- ✅ 文件读取异常
  - 验证错误处理机制
  - 验证默认返回值

**预期结果**:
- 所有异常情况都有降级处理
- 不会导致系统崩溃
- 错误日志正确记录

## 自动化测试结果

所有9项自动化测试已通过:

### 配置文件测试
- ✅ 模型路由配置 - 4条规则，包含所有类型
- ✅ 客户档案配置 - JSON元数据存储
- ✅ 技能提词器文件 - 3个示例文件
- ✅ 技能组合配置 - 3个组合

### 代码实现测试
- ✅ 路由引擎 - 完整匹配逻辑（关键词、文件类型、意图）
- ✅ 提词器组合器 - 动态拼接功能
- ✅ 管理后台集成 - 客户菜单和组件
- ✅ API路由 - 完整的CRUD操作

### 错误处理测试
- ✅ Try-Catch错误捕获
- ✅ 错误日志记录
- ✅ 默认返回值
- ✅ HTTP状态码

## 已知限制

1. **技能提词器界面**: 
   - 目前没有独立的"提词器"菜单项
   - 组合管理功能需要进一步集成到模型或内容管理模块

2. **路由规则测试**: 
   - 需要实际调用模型API才能完全验证路由效果
   - 当前测试只验证了配置和逻辑

## 下一步优化建议

### 高优先级
1. 创建独立的"提词器"菜单项
2. 在模型管理中集成路由规则测试界面
3. 添加路由规则的可视化编辑器

### 中优先级
1. 实现技能组合的可视化编辑
2. 添加提词器预览功能
3. 实现路由规则的批量导入/导出

### 低优先级
1. 路由规则的优先级拖拽调整
2. 客户档案的批量操作
3. 技能提词器的版本管理

## 测试数据清理

测试完成后，可以运行以下命令清理测试数据：

```bash
# 清理测试客户
rm -f /Users/liwei/Desktop/yinxin-rag-stack/data/customers/test-001.txt

# 重置客户档案配置
echo '{"archives":[]}' > /Users/liwei/Desktop/yinxin-rag-stack/data/customer-archives.json
```

## 报告问题

如果发现任何问题，请提供：
1. 操作的详细步骤
2. 预期行为 vs 实际行为
3. 浏览器控制台错误信息
4. 服务器终端日志
